@page "/Account/Manage/PersonalData"

@inject IdentityUserAccessor UserAccessor

<PageTitle>Osobní údaje</PageTitle>

<StatusMessage />
<h3>Osobní údaje</h3>

<div class="row">
    <div class="col-md-6">
        <p>Váš účet obsahuje osobní údaje, které jste nám poskytli. Na této stránce si je můžete stáhnout nebo smazat.</p>
        <p>
            <strong>Smazání těchto údajů trvale odstraní váš účet a akci nelze vrátit zpět.</strong>
        </p>
        <form action="Account/Manage/DownloadPersonalData" method="post">
            <AntiforgeryToken />
            <button class="btn btn-primary" type="submit">Stáhnout</button>
        </form>
        <p>
            <a href="Account/Manage/DeletePersonalData" class="btn btn-danger">Smazat</a>
        </p>
    </div>
</div>

@code {
    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    protected override async Task OnInitializedAsync()
    {
        _ = await UserAccessor.GetRequiredUserAsync(HttpContext);
    }
}
